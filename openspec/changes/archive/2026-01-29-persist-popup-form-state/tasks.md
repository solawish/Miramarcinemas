## 1. 實作
- [x] 1.1 定義表單狀態儲存鍵（例如 `popup_form_state`），並實作從 `chrome.storage.local` 讀取/寫入表單狀態的函式（電影 GUID、時間 value、票數、票種關鍵詞）
- [x] 1.2 在 popup 載入流程中：場次資料載入完成並更新電影/時間選單後，從儲存還原電影、時間、票數、票種關鍵詞；還原時間時須在已選電影的場次選項存在時才設定
- [x] 1.3 在電影、時間、票數的 `change` 事件與票種關鍵詞的 `input` 或 `change` 事件中，將當前值寫入儲存
- [x] 1.4 確認 manifest 已具備 `storage` 權限（Manifest V3 預設有 `chrome.storage.local`，無需額外宣告則僅需確認）

## 2. 驗證
- [ ] 2.1 手動測試：選擇電影、時間、票數並輸入票種關鍵詞後關閉 popup，再開啟，確認四個欄位皆還原
- [ ] 2.2 手動測試：還原後若該電影或場次已不存在於 API 結果，確認不會錯誤還原無效選項（時間選單依電影動態，需特別驗證）
